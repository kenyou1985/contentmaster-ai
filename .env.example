# --- 前端（Vite 暴露给浏览器，可选）---
# YouTube Data API v3：有 Key 时直连 Google，无 Key 时走本站 /api/invidious 代理
VITE_YOUTUBE_API_KEY=

# --- 仅 Vercel Serverless / 本地 dev（勿加 VITE_ 前缀）---
# Invidious 上游（搜索/频道/视频元数据）。勿填 MeTube 域名。
INVIDIOUS_UPSTREAM_URL=https://invidious.projectsegfau.lt

# Railway 上的 MeTube 根地址（仅用于 /api/metube/add 转发下载队列）
METUBE_URL=https://your-metube.up.railway.app
