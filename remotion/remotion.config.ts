// 内容已迁移到 server/remotion-server/render-worker.mjs 中通过 @remotion/bundler 调用
// @remotion/cli/config 不是必需的（仅用于命令行 npx remotion render）
// 如需 CLI 渲染，可执行: npm install --save-dev @remotion/cli
export {};
